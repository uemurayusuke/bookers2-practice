<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being
  <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<h1>Editing Book</h1>

<%= form_with model: @book do |f| %>


  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Update Book' %>
<% end %>
<p><%= link_to "Show", book_path(@book) %></p>
<p><%= link_to "Back", books_path %></p>